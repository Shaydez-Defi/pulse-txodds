"use client";

/** Browsing is open — wallet prompts live on predict / claim / profile actions only. */
export function WalletGate({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}